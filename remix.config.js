/**
 * NOTE: When the Remix Vite plugin is active, Remix does NOT read this file.
 * Options are passed to `vitePlugin(...)` in `vite.config.ts` instead.
 * This file is kept for reference / compatibility with non-Vite tooling.
 *
 * @type {import('@remix-run/dev').AppConfig}
 */
export default {
  serverModuleFormat: "esm",
  future: { v3_routeConvention: true },
};
